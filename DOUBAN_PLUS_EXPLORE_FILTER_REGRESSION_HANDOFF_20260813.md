# Douban Plus Explore 筛选回归交接文档

更新时间：2026-08-13  
工作副本：`D:\chatgpt\观影助手\开发\v1.0-douban-plus`  
交接性质：仅记录问题与排查边界，本轮未修复代码、未重建 EXE/ZIP。

## 一、用户确认的目标状态

1. 进入电视剧探索首页时，初始页面只显示豆瓣原生的三个一级入口：
   `全部`、`最近热门剧集`、`最近热门综艺`。
2. 初步筛选完成、并且真正进入“全部”模式后，才显示“全部”模式的完整筛选按钮。
3. 电视剧“全部”模式的完整筛选组应与豆瓣真实页面一致：
   `类型`、`地区`、`年代`、`平台`、`排序`。
4. 电影模块必须保持原有逻辑和视觉，不得因电视剧适配或共享筛选代码而出现按钮错位、筛选层提前出现、原生选项覆盖卡片等回归。
5. 电影和电视剧可以复用底层机制，但页面模式、原生 DOM 生命周期和当前筛选状态必须隔离，不能用一个页面的旧节点或旧标签污染另一个页面。

## 二、本次用户报告的实际 Bug

### 2.1 电视剧首页提前显示完整筛选按钮

截图：

- `C:\Users\leozw77\AppData\Local\Temp\codex-clipboard-7d5edde8-8be3-43e2-bc20-de63ea8a5b38.png`
- `C:\Users\leozw77\AppData\Local\Temp\codex-clipboard-56341c58-3eb3-416c-8dc0-586a9343c751.png`

现象：电视剧探索页一进入，一级入口下面已经出现 `类型`、`地区`、`年代`，或出现 `类型`、`华语`、`年代`、`平台`、`排序`。这不符合“首页只先显示三个按钮”的要求。

### 2.2 电视剧筛选组名称错误

现象：应显示 `地区` 的位置出现 `华语`。`华语`是选项值，不是筛选组标题，说明当前渲染过程中混用了筛选组标题和选项文本，或读取了过渡阶段/旧页面的节点。

### 2.3 电影模块发生回归

截图：

- `C:\Users\leozw77\AppData\Local\Temp\codex-clipboard-f4e1f348-2f99-440a-aced-006d7d65e846.png`
- `C:\Users\leozw77\AppData\Local\Temp\codex-clipboard-b8064d86-060b-44eb-b14c-d6f1d16b56ed.png`

现象：电影探索页的筛选栏出现错误显示；点击筛选后原生选项面板直接覆盖卡片区域，且首页状态下出现了不应提前显示的筛选按钮。当前共享筛选改动不能视为只影响电视剧，电影模块必须先恢复并重新验收。

### 2.4 当前构建结果不能作为 UI 已完成证明

此前静态校验、脚本嵌入校验、Release 编译和自检均通过，但这些检查没有覆盖真实 WebView2 中的“初始状态 → 点击全部 → 原生 DOM 切换完成 → 渲染筛选组 → 切回非全部模式”状态序列。因此此前“电视剧全部筛选已修正”的文档描述需要作废，不能继续作为验收结论。

## 三、已核实的豆瓣真实 DOM 事实

### 3.1 电视剧初始页面

真实 `https://movie.douban.com/tv/` 初始 DOM 已核实：

- 一级入口：`全部`、`最近热门剧集`、`最近热门综艺`；
- 非“全部”状态使用 `.explore-menu-second li.explore-menu-second-tag` 平级分类；
- 初始页面没有“全部”模式的五个 `.base-selector` 筛选组；
- 电视剧实际存在的复选筛选以实时 DOM 为准，不能从电影页面硬编码补入。

### 3.2 点击豆瓣原生“全部”之后

真实 DOM 已核实：

- `.explore-menu-second` 被替换/移除；
- `.explore-all-selectors-main` 内出现五个 `.base-selector`；
- 五个组标题依次为：`类型`、`地区`、`年代`、`平台`、`排序`；
- 下拉选项位于对应组的 `.expand-card .drc-label` 中；
- 因此“全部”模式必须等待原生切换完成后再读取和渲染，不能仅凭一级按钮文本提前判断。

## 四、当前实现的风险点与待验证根因

当前 `WebAssets\DoubanPlus\douban-explore-page.js` 的关键路径包括：

- `isAllMode()`：根据当前一级节点文本判断是否为“全部”；
- `native.secondary()`：在“全部”与非“全部”之间切换查询不同的 DOM；
- `allFilterGroups()`：读取 `.base-selector-title span` 作为筛选组标题；
- `renderSecondaryControlsWhenReady()`：等待筛选节点后调用渲染；
- `renderPrimaryTabs()`：点击一级入口后延时重新渲染。

这些路径目前存在以下待验证风险，下一次对话必须用实时 DOM/状态日志确认，不能继续猜测：

1. `isAllMode()` 在豆瓣原生异步切换尚未完成时就返回 `true`，导致适配器过早进入“全部”分支。
2. `renderSecondaryControlsWhenReady()` 目前只等待节点存在，没有确认节点属于当前页面模式、当前一级入口和当前一次切换代次。
3. 原生 DOM 替换期间，旧 `.base-selector`、旧选项面板或旧 `secondary` 节点可能短暂存在，适配器可能读取了过渡态节点。
4. `allFilterGroups()` 读取组标题与 `nativeGroupValue()` 读取当前值的边界不清晰；出现“华语”说明必须确认读取目标是否是组标题节点，而不是组内当前选项/选项面板文本。
5. 电影和电视剧共用 `native.secondary()` 后，页面切换或模式切换时可能保留旧闭包、旧索引或旧 `state.customFilterMenu`，从而造成电影 UI 被电视剧筛选状态污染。
6. 当前逻辑使用定时器重绘，但没有“DOM 稳定签名”或“切换代次”保护；仅靠等待 260ms/80ms 不能证明豆瓣筛选切换已经完成。

以上是排查方向，不是已经确认的最终根因。下一次必须先记录：当前 URL、contentType、一级选中项文本、`.explore-menu-second` 是否存在、五个 `.base-selector` 的标题签名、渲染代次，再实施修复。

## 五、下一次修复的实现边界

### 5.1 先恢复正确状态机，再复用代码

建议将 Explore 筛选生命周期明确分为：

| 状态 | 允许渲染的内容 |
|---|---|
| `primary-initial` | 只渲染当前页面的一级入口；电视剧首页只显示三个入口 |
| `primary-transition` | 隐藏旧筛选按钮，等待豆瓣原生 DOM 完成切换 |
| `non-all-ready` | 渲染当前非“全部”模式实际存在的平级分类 |
| `all-pending` | 一级入口已选择“全部”，但五组 DOM/标题签名未稳定；不渲染完整筛选按钮 |
| `all-ready` | 仅当五组标题均从当前 `.explore-all-selectors-main` 稳定读取后，渲染五个组按钮 |

核心要求：没有进入 `all-ready`，就不能显示 `类型/地区/年代/平台/排序` 这类完整筛选按钮。

### 5.2 电影与电视剧共享机制，但隔离运行状态

- 共享：等待 DOM 稳定、读取组标题、打开原生组、提取选项、点击原生选项、筛选后重置分页。
- 隔离：`contentType`、一级入口集合、当前 DOM 容器、切换代次、当前组节点、弹出菜单和重绘任务。
- 每次电影/电视剧切换或一级入口切换时，先销毁旧自定义弹层、取消旧重绘任务、清空旧筛选按钮，再重新建立当前页面状态。
- 组标题只允许从当前 `.explore-all-selectors-main` 的直接所属组读取；不得从组内选项文本、旧节点或跨页面缓存推导标题。

### 5.3 “全部”模式的显示时机

不得在页面初始加载时因为 `currentPrimary()` 恰好返回第一个节点就直接显示全部筛选组。至少要同时满足：

1. 当前一级入口确实是用户/豆瓣原生点击后的 `全部`；
2. 当前切换代次仍然有效；
3. `.explore-all-selectors-main` 属于当前文档并已出现；
4. 五个组的标题签名连续两次读取一致；
5. 没有旧的 `.explore-menu-second` 或旧弹层正在覆盖当前状态。

### 5.4 不允许把当前错误 EXE 当作回归基线

此前生成的 `215051` 版本包含当前共享筛选改动，已被用户现场证明存在电视剧和电影 UI 回归。下一次修复前，该包只能作为问题复现材料，不能作为稳定交付包或验收基线。

## 六、下一次必须执行的真实验收序列

静态测试和编译通过后，仍必须在登录态 WebView2 中完成以下序列：

1. 打开电视剧探索首页：只看到三个一级入口，不看到完整五组按钮。
2. 点击电视剧“全部”：等待原生切换完成后，准确看到 `类型/地区/年代/平台/排序` 五个按钮。
3. 逐一打开五个按钮：每个按钮的选项来自自己的 `.expand-card`，组标题不得变成 `华语` 等选项值。
4. 选择一个地区或年代：确认列表更新、按钮显示当前值、其他组不串值。
5. 切换回“最近热门剧集”或“最近热门综艺”：五组按钮消失，只显示当前非全部模式的真实平级分类。
6. 切换到电影：电影首页与原有基线一致，不出现电视剧的五组标签、不显示错误按钮、不残留弹层。
7. 电影进入“全部”及其他模式重复上述检查。
8. 电影与电视剧之间来回切换多次，确认不存在旧筛选、旧弹层、旧标题和卡片覆盖。
9. 详情进入/返回、无限滚动和待看操作作为回归项复测；双 WebView 详情返回机制不应被本次筛选修复改动。

## 七、文件范围与发布影响

下一次允许修改的主要文件：

- `WebAssets\DoubanPlus\douban-explore-page.js`
- 如确有必要：`WebAssets\DoubanPlus\douban-explore-page.css`
- `tests\validate_douban_explore.py`
- `DOUBAN_PLUS_EXPLORE_ADAPTER_HANDOFF_20260813.md`
- `AI_HANDOFF.md`
- `BUILD_STATUS.txt`

原则：不修改个人页、详情双 WebView、正式 v0.9.0 发布目录；除非回归证据明确证明共享宿主代码也受影响。

本轮交接结论：

- 当前工作副本记录为“Explore 筛选回归未解决”；
- 当前 EXE/ZIP 不作为本次问题的正式交付；
- 未生成新的 EXE/ZIP；
- 下一次必须先完成真实 WebView2 状态序列验收，再更新稳定状态和交付哈希。

## 八、用户指定回滚基线（2026-08-14）

用户明确指定以下包作为当前 Explore 筛选修复的可回滚基线。后续筛选重绘重做失败时，优先恢复此完整目录，不得只替换 EXE：

- 目录：`artifacts\观影助手-v1.0.1-dual-webview-explore-tv-ui-20260814-010555-win-x64`
- EXE：`artifacts\观影助手-v1.0.1-dual-webview-explore-tv-ui-20260814-010555-win-x64\观影助手.exe`
- ZIP：`artifacts\观影助手-v1.0.1-dual-webview-explore-tv-ui-20260814-010555-win-x64.zip`
- EXE SHA-256：`554210D3E9236BB106851814D3EEB1D76482A439C13A6CFBB82263BF596572FA`
- ZIP SHA-256：`51EB79FFC7185F53B6512A152F27947F14EEB90BE531D12B542452BCD0D39F71`
- Explore 脚本 SHA-256：`8140D8C53D9E41F8B566433AEF115B9109EC7C7F2AC3BF57AFF01A30FFEC7DF9`

该基线的已知状态：筛选弹窗选项已不再重复；“类型→排序”存在用户现场反馈的间歇性失效，因此它是**可回滚开发基线**，不是已验收稳定版本。该包的静态验证为 Explore `0 failed`、Release 编译 `0 警告/0 错误`、自检 `18/18` 与 `71/71`；真实 WebView2 筛选序列仍未完成验收。
