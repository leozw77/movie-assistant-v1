BuildFix12 R7

基线：BuildFix12 R6 + BuildFix11 R3 双 WebView2。

本轮修复：
- 根据 2026-08-09 09:43~09:44 实机诊断，豆瓣 /people/{profileId}/do 对“在看 + 评分 + 短评”可能表现为两阶段删除。
- 第一次官方列表真实鼠标点击后，短评内容可先消失，但 SubjectId 仍稳定存在于 /do 列表。
- R7 不再把这种情况立即返回给用户要求手工重试。
- 单次用户删除事务内部最多执行两次受控 Chromium Input.dispatchMouseEvent。
- 只有新鲜重载 /do 页面连续两次确认目标仍存在时，才允许自动第二击。
- 第二击前重新按 SubjectId 精确定位目标卡片和唯一删除控件。
- 最终仍要求 /do 列表目标消失 + 影片详情连续两次确认状态/评分/短评/日期为空 + do 列表硬复核。
- 最多两击；如果第二击后仍存在，返回未确认，不清本地。
- 不使用 fetch、HttpClient、手工 POST、Cookie 导出或私有写入 API。
