# Douban Plus v1.0.1 稳定版交接文档

日期：2026-08-13  
工作副本：`D:\chatgpt\观影助手\开发\v1.0-douban-plus`

## 一、版本结论

当前版本正式登记为 `v1.0.1`，状态为稳定版，稳定标签为 `stable-v1.0.1`。

v1.0.1 是当前可交付稳定版本，不是试用版、预览版或临时开发版。本版本基于正式 `v0.9.0 BuildFix12 R11` 业务基线开发；正式 v0.9.0 和历史 v1.0.0 的 EXE、ZIP、发布目录及哈希均保持不变，禁止覆盖。

用户已完成真实 UI 验收，确认个人页无限滚动正常，并确认进入详情页后返回能够恢复到原有位置。

## 二、v1.0.1 稳定版包含内容

- 同一窗口可见 WebView2 中的 Douban Plus 页面主线。
- 豆瓣个人页 `collect / wish / do` 解析、状态导航和个人页无限滚动。
- 个人页点击详情后按 SubjectId 恢复目标卡片和原视口位置；找不到目标卡片时使用 scrollY 兜底。
- 豆瓣搜索页当前页结果解析、卡片重绘和豆瓣原生分页。
- 搜索页卡片评分、评价人数、年份、事实信息、海报和详情链接展示。
- 真实豆瓣影片详情页同窗口导航和来源页返回。
- 本地 watchlist 独立存储与个人页、搜索页、详情页入口。
- 豆瓣官方评分、状态、短评写入、删除和官方回读确认后端继续保留。

## 三、已修复内容

### 1. 个人页返回位置

旧实现仅在返回后以固定延时恢复 scrollY。无限滚动内容尚未追加完成时，目标位置可能超过当前文档高度，导致返回位置丢失。

v1.0.1 使用以下恢复链路：

1. 保存 profile、status、筛选 scope、SubjectId、scrollY 和卡片视口偏移。
2. 返回后按 SubjectId 查找点击的目标卡片。
3. 目标卡片不存在时，按当前分页顺序继续加载，直到卡片出现或达到安全上限。
4. 按目标卡片位置恢复，并在布局稳定后再次校正。
5. 找不到目标卡片时回退到 scrollY。

### 2. 搜索卡片分隔符

修复搜索页 CSS 中错误的 `content: "路"`，恢复为正确的 `content: "·"`。该问题只影响视觉分隔符，不涉及搜索数据、分页和网络请求。

## 四、明确删减内容

以下内容已从当前主线删除或不再使用：

- 旧本地 HTML/原生影视库页面及其资源桥接。
- “从豆瓣导入历史”入口、历史分页同步和相关 CDP 回退。
- `douban-history.json`、`douban-history.before-cdp-repair.json`、`douban-search-cache.json` 的读写。
- 依赖历史缓存的旧 AI 问题/影评流程及专用测试。
- C# 演职员读取、人物 ID/链接、头像读取、补全、缓存和资源代理。
- 搜索页无限滚动实验，包括自动下一页 fetch、iframe fallback、sentinel 和自动追加。

## 五、明确未纳入 v1.0.1

- `movie.douban.com/explore` 选电影页适配。
- 搜索页无限滚动；搜索页继续使用当前页解析和豆瓣原生分页。
- 新的实时内容 AI UI；正式豆瓣评价写入、删除和回读后端继续保留。

## 六、验证与真实 UI 边界

静态和构建门禁：

- Douban Plus-only 源码检查：39 项通过。
- 嵌入 JavaScript 检查：通过。
- 个人页静态校验：通过。
- Release 编译：0 警告、0 错误。
- 评价专项自检：18/18。
- 综合自检：70/70。
- 正式 v0.9.0 和历史 v1.0.0 文件哈希：未变化。

真实 UI 边界：

- 用户已确认个人页无限滚动正常。
- 用户已确认个人页进入详情后返回原有位置正常。
- 登录状态、豆瓣网络、官方评价写入/删除和回读仍按具体功能单独验收。
- 静态检查和自检不替代真实豆瓣页面验收。

## 七、下一步开发

2026-08-13 的 Explore 开发线、当前返回问题和后续双 WebView 方案，见：

- `DOUBAN_PLUS_V1.0.1_STABLE_EXPLORE_RETURN_HANDOFF_20260813.md`
- `DOUBAN_PLUS_DUAL_WEBVIEW_PLAN_20260813.md`

当前稳定发布物的 `/explore` 纳入边界仍以 `STABLE_VERSION_V1.0.1.json` 为准；本次文档同步不覆盖既有 EXE、ZIP、发布目录或哈希。后续实现双 WebView 并完成真实登录态 UI 验收后，再单独决定是否生成新的稳定产物。

后续每次交付必须同时提供：最新稳定版 Release EXE、ZIP、构建结果、静态自检结果、真实 UI 验收边界和对应交接文档。
