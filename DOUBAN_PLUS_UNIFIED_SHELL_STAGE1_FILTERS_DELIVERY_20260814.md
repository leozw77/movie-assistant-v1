# Douban Plus unified Shell：Explore 筛选与分页交付说明

日期：2026-08-14

## 本轮实现

本轮仍保持“统一 Shell 显示、真实豆瓣 Explore DOM 提供内容”的架构：

- Shell 增加豆瓣原生分类、标签和筛选组入口。
- Shell 点击筛选后，通过 C# JSON 消息桥驱动隐藏 Source WebView 中豆瓣真实筛选节点。
- Source WebView 等待豆瓣 DOM 结果稳定，再读取 `DOM → JSON`，回传 Shell 重绘卡片。
- Shell 增加豆瓣原生“加载更多”入口；点击后由 Source WebView 点击豆瓣原生按钮并等待新卡片出现。
- 海报卡片改为 `2:3` 完整比例并使用 `contain`，不再把海报强行裁成固定短图。
- 详情仍沿用现有双 WebView 详情入口和返回边界；本轮没有改动详情页返回状态机制。

## 明确没有做

- 没有恢复旧的隐藏 DOM 代理页面。
- 没有创建本地假筛选、假分页或直接调用豆瓣接口。
- 电视剧、个人影片、我的待看、无限滚动仍未纳入本轮。
- 真实登录态、豆瓣页面 DOM 版本变化和网络可用性仍需用户在 EXE 中验收。

## 交付物

目录：

`artifacts/观影助手-v1.0.1-unified-shell-stage1-20260814-122327-win-x64/`

- EXE：`观影助手.exe`
- ZIP：同名目录旁的 `观影助手-v1.0.1-unified-shell-stage1-20260814-122153-win-x64.zip`
- EXE SHA256：`E533C09997E841810C0638A20712DEC0988A6B5108FFD05BE1E5C2123C638F3C`
- ZIP SHA256：`79FD8165A056E1426C3A604CD5081DD61B3BD7166F6E3DBFE1FA21FE1C4D7970`

本交付为独立试验目录，不覆盖正式 v0.9.0、稳定 v1.0.1 或 `010555` 回滚目录。

## 用户测试步骤

1. 解压 ZIP 或直接运行 EXE，完成豆瓣登录；进入“探索电影”。
2. 首屏确认：顶部仍是统一 Shell，豆瓣原始 Explore 页面不在可见区域闪现；卡片海报完整显示。
3. 点击一个一级分类或标签，确认按钮进入等待状态，之后卡片和顶部数量发生对应变化。
4. 点击筛选组（例如年份、地区、类型等实际出现的组），确认 Shell 展开该组从豆瓣 DOM 读取的选项；选择一个选项，确认卡片重新读取。
5. 点击底部“加载更多”（如果豆瓣当前页面提供该按钮），确认卡片数量增加且页面仍保持统一 Shell；如果豆瓣已无按钮，Shell 应显示已到当前结果末页。
6. 点击任意卡片进入详情；返回后确认仍回到统一 Shell，筛选后的卡片、筛选状态和滚动位置不被清空。
7. 再切换一次筛选并重复“加载更多”，排除只在首次操作时有效的假成功。

## 结果记录

若筛选或分页失败，请记录：

- 点击的筛选组和选项文字；
- Shell 顶部状态文字；
- 是否出现“豆瓣原始页面”闪现；
- 卡片数量变化；
- 详情返回后筛选状态和滚动位置；
- `%LOCALAPPDATA%\\DoubanBrowserReminder\\logs\\diagnostic.log` 中对应时间段的 `Unified Shell` 日志。

静态检查、C# 编译和自检只能证明协议与构建完整，不能替代上述真实登录 WebView2 验收。
