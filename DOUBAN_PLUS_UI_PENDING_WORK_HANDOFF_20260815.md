# Douban Plus 稳定基线与待办交接文档

> 历史副本：本文件保留当日交接记录，不是当前基线来源。当前稳定版、配套源码、回滚基线和允许构建目录以父目录 `DEVELOPMENT_BASELINE.json`、`DEVELOPMENT_DIRECTORY_INDEX.md` 和 `AGENTS.md` 为准；不要使用本文件中的旧稳定版路径或旧哈希直接构建。

日期：2026-08-15  
工作副本：`D:\chatgpt\观影助手\开发\v1.0-douban-plus-unified-shell-20260814`

## 一、当前结论

本轮停止悬浮卡片方案。

最后一个稳定版本继续作为后续开发基线；悬浮海报、悬浮详情面板和相关诊断版本均属于实验版本，不纳入稳定基线，也不作为后续 UI 设计基础。

本轮只完成交接记录，不修改代码、不回滚源码、不生成新的功能版 EXE。

## 二、后续开发基线

稳定版 EXE：

`D:\chatgpt\观影助手\开发\v1.0-douban-plus-unified-shell-20260814\artifacts\观影助手-v1.0.1-unified-shell-stable-主演-20260815-061246.exe`

SHA256：

`CA26B6ACDC07268D2D1E844CB5979846A36D1A53964884D07E5108AD74E8AC78`

对应构建目录：

`D:\chatgpt\观影助手\开发\v1.0-douban-plus-unified-shell-20260814\artifacts\观影助手-v1.0.1-unified-shell-stage1-20260815-061246-win-x64`

稳定版构建记录：

- Release 编译：0 错误；
- Review pipeline：18/18；
- 内置综合自检：72/72；
- 构建时间：2026-08-15 06:12:57 +08:00；
- 真实登录态 WebView2 页面仍需按用户现场环境验收，静态自检不替代真实页面验收。

后续如果继续开发，应从上述稳定 EXE 对应的源码状态建立新的开发分支或复制目录。不要从 `hover-poster-v1/v2/v3/v4` 实验产物继续叠加。

## 三、稳定版已经具备的范围

- 统一 `DoubanShell + DOM Source WebView + C# JSON 消息桥` 架构；
- 探索电影与电视剧的统一 Shell 卡片承载；
- 个人影片页面统一 Shell 承载；
- 电影/电视剧切换入口；
- 个人页 `看过 / 想看 / 在看` 状态入口和分页链路；
- 个人页连续加载、SubjectId 去重和详情返回位置恢复；
- Explore 当前筛选、分页、卡片和详情链路的基础闭环；
- 海报使用直连 URL，失败时保留宿主兜底；
- 卡片标题、年份/地区、类型、导演、主演和评分的统一数据映射；
- 详情页可在统一应用层级之外显示，返回后列表状态由 Shell 保留；
- 短评入口保留为卡片底部的短评提示；
- 正式豆瓣评分、状态、短评写入、删除和官方回读相关后端能力继续保留。

## 四、明确不纳入稳定版的悬浮实验

以下版本不作为稳定版或后续 UI 基础：

- `观影助手-v1.0.1-unified-shell-hover-poster-20260815-062103.exe`；
- `观影助手-v1.0.1-unified-shell-hover-poster-v2-20260815-062855.exe`；
- `观影助手-v1.0.1-unified-shell-hover-poster-v3-20260815-063434.exe`；
- `观影助手-v1.0.1-unified-shell-hover-poster-v4-20260815-064237.exe`；
- `douban-plus-hover-fix-v4.exe`。

悬浮实验的问题是：卡片信息被拆到海报覆盖层后，实际 WebView2 中的鼠标悬浮表现不稳定，且会使卡片信息层级变得过于隐蔽。本项目后续只保留短评悬浮的设计方向；其他类型、导演、主演、地区和评分信息不再放入悬浮层。

## 五、当前未完成事项

### 1. 个人页面筛选未完成

当前个人页面已有状态入口，但个人页面的完整筛选能力尚未实现。后续需要明确：

- 筛选对象是当前状态下的个人影片；
- 筛选条件应绑定个人页面当前状态和当前内容类型；
- 筛选后应重置分页游标、当前卡片集合和滚动恢复状态；
- 筛选后进入详情并返回，必须回到筛选后的列表和位置；
- 不能把 Explore 的筛选状态、选项或 DOM 容器复用到个人页面。

### 2. Explore 筛选 UI 尚未改为横版

当前 Explore 筛选仍存在纵向排列和大面积空白的问题，未达到统一 UI 目标。

后续目标：

- 每个筛选组采用横向标签行；
- 筛选组名称固定在行首；
- 选项从左到右排列，空间不足时自然换行；
- 不改变豆瓣原生筛选的实际选项、选中状态和提交链路；
- 电影和电视剧共用视觉组件，但分别绑定各自的 Source DOM 状态；
- 筛选面板收起后只保留当前选中条件的简短摘要。

这项工作只先改 Shell 展示层，不能重新引入隐藏 DOM 代理或自造筛选数据。

### 3. Explore 评分区间未完成

当前 Explore 尚未接入评分区间筛选。

后续需要先确认豆瓣真实 Explore 页面中评分区间的原生 DOM、选项值、提交事件和结果回读方式，再接入统一 Shell。评分区间必须：

- 来源于真实豆瓣 Source DOM；
- 与当前电影/电视剧模式绑定；
- 点击后只提交一次原生筛选；
- 筛选成功后清空旧卡片并重置分页；
- 筛选失败时恢复上一次稳定状态并显示错误；
- 不使用固定延时猜测提交完成。

### 4. “我的待看”尚未接入

顶部“我的待看”入口目前未完成统一 Shell 接入。

后续应明确它是：

- 豆瓣个人页“想看”状态的统一入口；
- 还是本地 Watchlist 的独立入口。

在没有明确边界前，不要把本地待看数据和豆瓣个人页“想看”数据混合。若复用个人页“想看”，必须复用个人页状态、分页、详情返回和登录态；若保留本地 Watchlist，则必须单独标注数据来源。

## 六、建议实施顺序

1. 从稳定版源码建立新的 UI 开发基线，确认悬浮实验代码不进入本轮；
2. 先完成个人页面筛选的数据状态和返回边界；
3. 再完成 Explore 筛选 UI 横版布局，只调整 Shell 展示层；
4. 接入 Explore 评分区间，先完成电影，再按真实 DOM 差异适配电视剧；
5. 明确“我的待看”的数据来源后接入；
6. 所有功能完成后统一处理字号、卡片高度、评分位置、海报比例和响应式布局；
7. 最后再生成新的稳定版 EXE。

不要在筛选数据、分页状态和待看来源尚未稳定前继续做大规模视觉重构。

## 七、每项功能的验收序列

### 个人页面筛选

登录豆瓣 → 进入个人页面 → 切换“看过/想看/在看” → 应用筛选 → 加载更多 → 进入详情 → 返回 → 确认筛选条件、卡片列表、分页和滚动位置均保留。

### Explore 横版筛选 UI

进入探索电影 → 打开筛选 → 检查筛选组横向排列、换行和收起状态 → 选择一个条件 → 确认卡片结果和选中摘要 → 切换电视剧重复验证，不能出现电影筛选污染电视剧。

### Explore 评分区间

进入电影 Explore → 打开评分区间 → 选择一个区间 → 确认只提交一次 → 确认结果变化 → 加载更多 → 进入详情 → 返回。电视剧需要单独验证真实原生选项是否存在。

### 我的待看

确认数据来源 → 进入“我的待看” → 查看卡片 → 进入详情 → 返回 → 验证分页、状态和数据来源标识；未登录时验证明确提示，不显示伪造数据。

## 八、后续交付要求

每次后续实现交付必须说明：

- 使用的稳定基线及其 SHA256；
- 修改的源码文件；
- 具体完成了哪些功能；
- 尚未完成和明确未纳入的内容；
- 静态检查、构建、自检结果；
- 用户需要现场验证的真实 WebView2 序列；
- 实际 EXE 完整路径和 SHA256；
- 不提供 ZIP，除非用户另行要求。
