# Douban Plus v1.0.1 稳定版交接补充：Explore 与详情返回

日期：2026-08-13  
工作副本：`D:\chatgpt\观影助手\开发\v1.0-douban-plus`  
版本基线：`v1.0.1` / `stable-v1.0.1`

## 一、交接结论

本文件是 `DOUBAN_PLUS_V1.0.1_STABLE_HANDOFF_20260813.md` 的补充，不覆盖原稳定版交接记录。

当前工作副本已经接入 Douban Plus 的 `movie.douban.com/explore` 选电影页适配，包含：

- “探索选片”入口；
- 全部、热门电影、最新电影、豆瓣高分、冷门佳片五种模式；
- 按豆瓣原生规则代理的类型、地区、年代、排序、评分区间、未看过、可播放筛选；
- 现有影视库风格的卡片重绘和原生加载更多；
- 详情入口及现有“加入本地待看”右键菜单。

但当前详情打开仍复用同一个可见 WebView。用户已观察到：从 Explore 卡片进入详情后，返回可能回到热门电影默认状态，或偶发进入恢复失败页面；右键“返回首页”可以成功回到个人页。该问题尚未达到新的稳定版真实 UI 验收标准，因此不能把当前 Explore 返回行为写成已验收稳定能力。

## 二、当前 v1.0.1 发布边界

- 版本文件和稳定标签保持 `1.0.1` / `stable-v1.0.1`。
- 正式 `v0.9.0 BuildFix12 R11` 不修改。
- 已有 v1.0.0、v1.0.1 EXE、ZIP、发布目录和哈希不覆盖。
- 本次只同步交接文档，不生成新的稳定包。
- `/explore` 当前实现属于 v1.0.1 工作副本的开发能力；正式稳定产物是否纳入，以 `STABLE_VERSION_V1.0.1.json` 和后续真实 UI 验收为准。

## 三、返回问题根因摘要

当前宿主结构是：

| 组件 | 当前用途 |
|---|---|
| `_doubanPlusView` | 唯一可见的 Douban Plus WebView，承载个人页、搜索页、Explore 和详情页 |
| `_detailView` | `1×1` 隐藏的详情/评价后端连接器，不是可见详情窗口 |
| `_workerView` | 后台请求和连接器工作 WebView |

点击 Explore 卡片时，宿主执行的是在 `_doubanPlusView` 内直接导航到 subject URL；返回时再依赖 WebView 历史、`_activeDoubanReturnUrl` 和 Explore 的 `sessionStorage` 恢复。由于 Explore 的筛选条件主要存在页面 DOM/脚本状态中，不写入 URL，回退到 `/explore` 只能得到豆瓣默认列表，无法可靠恢复用户当时的模式、筛选、评分区间、加载数量和滚动位置。

因此，问题不是简单把返回 URL 改成 `/explore`，也不是继续增加延时。根本问题是列表页面和详情页面共用了同一个可见导航栈，列表状态会被详情导航破坏。

## 四、后续方案入口

下一阶段按 `DOUBAN_PLUS_DUAL_WEBVIEW_PLAN_20260813.md` 实施双 WebView：列表 WebView 保留 Explore/个人页/搜索页的 DOM 和状态，详情 WebView 独立承载影片详情；返回时只切换可见控件，不重新导航列表页。

在双 WebView 完成前，不应继续用“返回后重新导航 Explore 再模拟恢复”作为长期方案。

## 五、交接时必须保留的验证边界

静态检查、Release 编译、内置自检和已生成测试包只能证明源码和宿主流程具备条件，不能替代真实登录态 WebView2 验收。下一次实现必须实测：

1. Explore 筛选后打开详情，再返回，模式、类型/地区/年代/排序、评分区间文字、未看过/可播放状态保持不变。
2. 已加载的卡片数量、卡片内容和滚动位置保持不变，不回到热门电影默认状态。
3. 个人页、搜索页、Explore 三类来源均能进入详情并返回各自来源页。
4. 顶部返回按钮、右键返回首页、页面刷新和 WebView2 恢复路径不互相破坏。
5. 发生导航失败时仍可通过右键刷新页面和返回首页恢复，且不显示无法操作的黑屏错误页。

