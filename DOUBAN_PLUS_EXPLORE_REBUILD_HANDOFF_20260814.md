# Douban Plus Explore 筛选重建交接文档

日期：2026-08-14  
工作副本：`D:\chatgpt\观影助手\开发\v1.0-douban-plus`  
交接性质：下一轮实现计划、页面统一边界与交付要求

## 一、当前结论

Explore 筛选目前不能视为完成。用户现场确认：

- 筛选弹窗选项已经不再重复；
- “类型 → 排序”存在间歇性失效；
- 失败表现具有时序性，说明问题不是单纯按钮样式，而是豆瓣原生筛选 DOM 异步重绘与当前适配器状态管理之间存在竞争。

当前必须保留的回滚基线：

- 目录：`artifacts\观影助手-v1.0.1-dual-webview-explore-tv-ui-20260814-010555-win-x64`
- EXE：`artifacts\观影助手-v1.0.1-dual-webview-explore-tv-ui-20260814-010555-win-x64\观影助手.exe`
- ZIP：`artifacts\观影助手-v1.0.1-dual-webview-explore-tv-ui-20260814-010555-win-x64.zip`
- EXE SHA-256：`554210D3E9236BB106851814D3EEB1D76482A439C13A6CFBB82263BF596572FA`
- ZIP SHA-256：`51EB79FFC7185F53B6512A152F27947F14EEB90BE531D12B542452BCD0D39F71`

该版本是可回滚开发基线，不是最终验收版本。失败时必须恢复完整目录，不能只替换 EXE；Explore 实际脚本位于同目录的 `WebAssets\DoubanPlus`。

## 二、下一步必须实现的功能

### 2.1 Explore 筛选状态机

把当前“点击后定时轮询列表签名”的逻辑重做为原生 DOM 状态机：

```text
primary-ready
  → all-transition / non-all-transition
  → native-filter-dom-ready
  → custom-filter-ready
  → native-option-opening
  → native-option-selected
  → native-dom-settling
  → filter-applied
```

每一次状态都必须绑定当前 `contentType`、当前一级入口、当前切换代次和当前原生 DOM 容器。旧节点、旧弹层和旧异步任务不得继续影响新状态。

### 2.2 原生 DOM 重绘处理

实现规则：

1. “全部”模式只从当前文档的 `.explore-all-selectors-main` 读取组。
2. 组标题只读取当前组直接的 `.base-selector-title`，不能从选项文本推导。
3. 选项只读取对应当前组的 `.expand-card .drc-label`。
4. 豆瓣替换筛选容器后，重新获取新的 `.base-selector`，不能继续使用旧闭包节点。
5. 同时观察筛选容器父节点和影片列表；筛选容器重建时重新绑定观察目标。
6. 不能把“列表签名发生变化”作为筛选成功的唯一条件；排序即使列表内容暂时相同，也必须能根据原生选中值和 DOM 稳定状态确认。
7. 需要连续稳定读取当前组标题、当前选中值和其他组值，并增加短暂 DOM 安静窗口，确认豆瓣重绘已经结束。
8. 一次筛选操作未完成前，禁止开始下一次筛选；新操作开始时取消旧 observer、timer 和等待任务。

### 2.3 原生点击链路

优先保持一次操作只触发一次原生点击，不再依赖连续派发多个合成鼠标事件来模拟真实交互。若豆瓣原生事件确实要求可信输入，再评估由宿主 WebView2 通过 CDP `Input.dispatchMouseEvent` 执行真实输入，并逐次等待调用完成。

这部分必须先通过真实登录态 WebView2 验收，不能只凭 JavaScript 语法或静态断言判断成功。

### 2.4 自定义筛选 UI 更新

- 筛选进行中只显示等待状态，不重建整组主筛选按钮。
- 筛选完成后优先原地更新按钮文字和激活状态。
- 只有电影/电视剧切换、一级入口切换或筛选组数量真正变化时，才重建筛选按钮。
- 自定义按钮事件应采用稳定的组标识和事件委托，避免每次筛选后产生旧闭包和新闭包竞争。

## 三、电影与电视剧目标状态

### 电影

- 初始页面保持原有电影一级入口和页面视觉。
- 进入“全部”后按真实 DOM 显示四组：`类型`、`地区`、`年代`、`排序`。
- 类型、地区、年代、排序可以任意顺序连续选择。
- 选择类型后再选排序、选择排序后再选类型，均必须生效。

### 电视剧

- 首页只显示三个一级入口：`全部`、`最近热门剧集`、`最近热门综艺`。
- 进入“全部”并等待原生 DOM 稳定后，显示五组：`类型`、`地区`、`年代`、`平台`、`排序`。
- 电视剧非“全部”模式继续读取豆瓣实际存在的平级筛选和复选项，不从电影页面硬编码补入。
- 电视剧筛选状态不得污染电影页面。

## 四、页面统一方案必须保留

本次重做筛选不能破坏最初确定的页面统一方向。

### 4.1 统一页面导航

可见页面统一为同级导航：

```text
看过 | 想看 | 在看 | 探索
```

Explore 内继续提供：

```text
电影 | 电视剧
```

个人页和 Explore 页面使用统一的全局搜索入口；详情页才显示统一文案“返回”，列表页不显示无意义的返回按钮。

### 4.2 统一卡片

个人页、Explore 页和本地待看页继续共用：

- `WebAssets\DoubanPlus\douban-card.js`
- `WebAssets\DoubanPlus\douban-card.css`

统一字段顺序：

```text
标题 → 类型/年份 → 辅助信息 → 状态/评分/日期
```

页面只负责数据映射，不复制卡片 DOM。电视剧接入和新增字段优先改映射，不改变三处页面的统一结构。

### 4.3 双 WebView 详情返回

- 列表 WebView 保留个人页、搜索页和 Explore 的 DOM、筛选状态、已加载卡片和滚动位置。
- 独立详情 WebView 负责可见影片详情。
- 进入详情不重新导航列表，不销毁列表状态。
- 返回只切换可见 WebView，不重新点击筛选、不用固定延时重建列表。
- 现有后台 `_detailView` / `_workerView` 评价和请求职责不改变。

本轮只重做 Explore 筛选，不得顺带修改个人页、搜索页、详情双 WebView 或正式 v0.9.0 发布物。

## 五、实现文件范围

首选只修改：

- `WebAssets\DoubanPlus\douban-explore-page.js`
- `tests\validate_douban_explore.py`

只有在真实输入链路确实需要宿主支持时，才允许扩展：

- `DoubanPlusWebView2Script.cs`
- `HtmlMediaLibraryForm.cs`

不得修改：

- 正式 `v0.9.0` 发布目录和 EXE/ZIP；
- 个人页和搜索页既有稳定逻辑；
- `_detailView` / `_workerView` 后台评价与请求协议；
- 共享卡片 renderer，除非出现明确的跨页面 UI 回归证据。

## 六、必须执行的真实验收序列

静态校验和编译通过不等于完成。真实登录态 WebView2 中必须执行：

1. 电视剧首页确认只有三个一级入口。
2. 电视剧点击“全部”，等待后确认五组完整筛选出现。
3. 逐一打开五组，确认每个弹窗只显示自己的选项，组标题不出现“华语”等选项值。
4. 电视剧执行“类型 → 筛选 → 排序”。
5. 电视剧执行“排序 → 筛选 → 类型”。
6. 重复上述两组序列至少多次，确认不是偶然成功。
7. 切回电视剧非“全部”模式，确认五组消失且平级筛选正确。
8. 切换到电影，确认首页视觉和筛选入口不被电视剧污染。
9. 电影执行“类型 → 筛选 → 排序”和“排序 → 筛选 → 类型”。
10. 电影与电视剧来回切换，确认无旧弹层、旧标题、旧节点和旧事件残留。
11. 筛选后进入详情再返回，确认筛选状态、卡片和滚动位置保留。
12. 复测 Explore 加载更多、个人页、搜索页、待看入口和详情返回，确保页面统一方案没有回归。

## 七、EXE/ZIP 交付要求

每次实现完成后必须执行项目既定构建脚本，生成新的不可覆盖时间戳目录：

```powershell
powershell.exe -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\Build-Preview.ps1
```

交付必须包含：

- 完整 EXE 路径；
- 完整 ZIP 路径；
- EXE SHA-256；
- ZIP SHA-256；
- Explore JS SHA-256；
- Explore 静态验证结果；
- Release 警告/错误结果；
- 自检结果；
- 真实 WebView2 验收结果和未覆盖边界。

没有生成 EXE/ZIP 时，必须明确说明“本轮未生成交付包”。不能用“编译通过”代替 EXE 交付，也不能用静态测试代替真实筛选验收。

## 八、完成判定

只有同时满足以下条件，才可描述为 Explore 筛选修复完成：

- 电影和电视剧按真实 DOM 正确显示筛选组；
- 类型与排序可以任意顺序连续生效；
- 多次重复序列稳定成功；
- 没有选项重复、组标题污染、旧节点覆盖或电影 UI 回归；
- 页面统一导航、共享卡片、双 WebView 详情返回没有回归；
- Release EXE/ZIP 已生成并提供哈希；
- 用户完成真实登录态 WebView2 验收。

在此之前，`20260814-010555` 始终保留为回滚基线，不能覆盖或删除。
